function reverseWord(str){
    let i=1
    let n=str.length;
    while(n <= n.length || n[i]!==" "){
        str = str.split("")
        console.log(str)
    }
}

let str = "have a nice day"
reverseWord(str)